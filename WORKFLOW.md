# 项目工作流文档

## 概述

本文档通过图形化方式展示 Telegram 双向机器人的完整工作流程，帮助理解系统架构和消息流转过程。

---

## 1. 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户层 (Telegram)                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  普通用户    │  │  管理员      │  │  管理员群组          │  │
│  │  (私聊)      │  │  (私聊)      │  │  (话题讨论)          │  │
│  └──────┬──────┘  └──────┬──────┘  └──────────┬──────────┘  │
└─────────┼────────────────┼────────────────────┼─────────────┘
          │                │                    │
          ▼                ▼                    ▼
┌─────────────────────────────────────────────────────────────┐
│                    Cloudflare Workers                         │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  入口层 (src/index.js)                                   │ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐ │ │
│  │  │  初始化数据库 │  │  路由分发    │  │  错误处理        │ │ │
│  │  │  dbInit()   │  │  route()    │  │  try/catch      │ │ │
│  │  └─────────────┘  └─────────────┘  └─────────────────┘ │ │
│  └─────────────────────────────────────────────────────────┘ │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  路由层 (src/router/index.js)                            │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │ │
│  │  │ GET /    │  │GET /verify│  │POST /    │  │ 404     │ │ │
│  │  │ 状态页   │  │ 验证页面  │  │submit_   │  │ 其他    │ │ │
│  │  │          │  │          │  │token     │  │         │ │ │
│  │  └──────────┘  └──────────┘  └────┬─────┘  └─────────┘ │ │
│  │                                    │                    │ │
│  │  ┌─────────────────────────────────┘                    │ │
│  │  │ Webhook 校验 (X-Telegram-Bot-Api-Secret-Token)       │ │
│  │  │ Update 幂等去重 (processed_updates 表)               │ │
│  │  └─────────────────────────────────┐                    │ │
│  │                                    ▼                    │ │
│  │  ┌───────────────────────────────────────────────────┐ │ │
│  │  │  Update 分发器 (src/handlers/update.js)            │ │ │
│  │  └───────────────────────────────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────┘ │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  处理器层 (src/handlers/)                                │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │ │
│  │  │ private  │  │adminReply│  │ callback │  │  edit   │ │ │
│  │  │ 私聊处理  │  │管理员回复 │  │ 回调处理  │  │编辑消息 │ │ │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬────┘ │ │
│  │       │             │             │             │      │ │
│  │  ┌────┴─────┐  ┌────┴─────┐  ┌────┴─────┐  ┌──┴────┐ │ │
│  │  │验证流程  │  │备注管理  │  │屏蔽/解封 │  │       │ │ │
│  │  │命令处理  │  │消息转发  │  │配置面板  │  │       │ │ │
│  │  │消息转发  │  │          │  │未读已阅  │  │       │ │ │
│  │  └─────────┘  └─────────┘  └─────────┘  └───────┘ │ │
│  └─────────────────────────────────────────────────────────┘ │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  服务层 (src/services/)                                  │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │ │
│  │  │ relay    │  │ inbox    │  │blacklist │  │ backup  │ │ │
│  │  │消息转发  │  │未读通知  │  │黑名单管理│  │消息备份 │ │ │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬────┘ │ │
│  │  ┌────┴─────────────┴─────────────┴─────────────┘      │ │
│  │  │                                                      │ │
│  │  │  ┌──────────┐  ┌──────────┐  ┌──────────┐          │ │
│  │  │  │ topic    │  │infoCard  │  │verification│        │ │
│  │  │  │话题创建  │  │资料卡    │  │验证服务    │        │ │
│  │  │  └──────────┘  └──────────┘  └──────────┘          │ │
│  │  └────────────────────────────────────────────────────  │ │
│  └─────────────────────────────────────────────────────────┘ │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  安全层 (src/security/)                                  │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │ │
│  │  │ webhook  │  │rateLimit │  │idempotency│  │initData │ │ │
│  │  │Webhook校验│  │限流控制  │  │幂等去重   │  │验签     │ │ │
│  │  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐ │ │
│  │  │regexGuard│  │ cleanup  │  │antiHarassment        │ │ │
│  │  │ReDoS防护 │  │TTL清理   │  │本地反骚扰检测         │ │ │
│  │  └──────────┘  └──────────┘  └──────────────────────┘ │ │
│  │  ┌──────────────────────┐  ┌──────────┐              │ │
│  │  │aiAntiHarassment      │  │aiSpamPrompt│             │ │
│  │  │AI反骚扰检测          │  │LLM提示词   │             │ │
│  │  └──────────────────────┘  └──────────┘              │ │
│  │  ┌──────────────────────┐  ┌──────────────────────┐ │ │
│  │  │aiAntiHarassment      │  │aiSpamPrompt          │ │ │
│  │  │AI 反骚扰检测           │  │LLM 提示词             │ │ │
│  │  └──────────────────────┘  └──────────────────────┘ │ │
│  │  ┌──────────────────────┐                           │ │
│  │  │aiMemory              │                           │ │
│  │  │AI 长期记忆管理        │                           │ │
│  │  └──────────────────────┘                           │ │
│  └─────────────────────────────────────────────────────────┘ │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  数据层 (src/database/)                                  │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │ │
│  │  │  config  │  │  users   │  │ messages │  │ updates │ │ │
│  │  │ 配置表   │  │ 用户表   │  │ 消息表   │  │Update记录│ │ │
│  │  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │ │
│  │  ┌──────────┐  ┌──────────┐                            │ │
│  │  │rateLimits│  │  index   │                            │ │
│  │  │限流记录  │  │DB初始化  │                            │ │
│  │  └──────────┘  └──────────┘                            │ │
│  │  ┌──────────┐                                           │ │
│  │  │  trust   │                                           │ │
│  │  │AI信任数据│                                           │ │
│  │  └──────────┘                                           │ │
│  └─────────────────────────────────────────────────────────┘ │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  API 层 (src/api/)                                       │ │
│  │  ┌──────────┐  ┌──────────┐                            │ │
│  │  │ telegram │  │ commands │                            │ │
│  │  │TG API封装│  │命令注册  │                            │ │
│  │  └──────────┘  └──────────┘                            │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据层 (Cloudflare D1)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │  config  │  │  users   │  │ messages │  │processed_│   │
│  │ 配置表   │  │ 用户表   │  │ 消息记录 │  │ updates  │   │
│  │          │  │          │  │          │  │ 去重记录 │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
│  ┌──────────┐                                              │
│  │rateLimits│                                              │
│  │ 限流记录 │                                              │
│  └──────────┘                                              │
│  ┌──────────┐                                              │
│  │  user_trust  │                                            │
│  │(信任表-AI/Green│                                            │
│  │   共享)     │                                            │
│  └──────────┘                                              │
│  ┌──────────┐  ┌──────────┐                                │
│  │ai_corrections││ ai_rules │                                │
│  │纠正记录表  │  │规则摘要表│                                │
│  └──────────┘  └──────────┘                                │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 消息流转图

### 2.1 用户发送消息流程

```
用户发送消息
    │
    ▼
┌─────────────────┐
│  Telegram API   │
│  (Webhook 推送) │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│  Webhook 校验   │────▶│  非法请求       │
│  Secret Token   │     │  返回 403       │
└────────┬────────┘     └─────────────────┘
         │ 通过
         ▼
┌─────────────────┐     ┌─────────────────┐
│  Update 幂等    │────▶│  重复请求       │
│  去重检查       │     │  返回 OK        │
└────────┬────────┘     └─────────────────┘
         │ 新请求
         ▼
┌─────────────────┐
│  限流检查       │
│  (用户/全局)    │
└────────┬────────┘
          │ 通过
          ▼
┌─────────────────┐     ┌─────────────────────────────────┐
│  反骚扰检测      │────▶│  触发规则                        │
│  (用户身份)     │     │  提示"不符合聊天对象"，不拉黑     │
└────────┬────────┘     └─────────────────────────────────┘
          │ 通过
          ▼
┌─────────────────┐     ┌─────────────────┐
│  用户是否被屏蔽  │────▶│  返回屏蔽提示   │
└────────┬────────┘     └─────────────────┘
         │ 未屏蔽
         ▼
┌─────────────────┐
│  验证状态检查   │
│  (new/pending/  │
│   verified)     │
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌───────┐ ┌───────────┐
│ 未验证  │ │ 已验证    │
│发送验证│ │           │
│流程    │ │ 本地反骚扰 │
└───────┘ │ (消息内容) │
           │           │
           │ 智能反骚扰 │
           │ (AI       │
           │ 检测)     │
           │           │
           │ 屏蔽词检测│
          │ 类型过滤  │
          │           │
          │ 自动回复  │
          │           │
          │ 忙碌模式  │
          └─────┬─────┘
                │
                ▼
          ┌───────────┐
          │ 转发到话题 │
          │ (relay)   │
          └─────┬─────┘
                │
        ┌───────┼───────┐
        ▼       ▼       ▼
   ┌────────┐┌──────┐┌────────┐
   │创建话题││未读通知││消息备份│
   │(topic) ││(inbox) ││(backup)│
   └────────┘└──────┘└────────┘
                │
                ▼
          ┌───────────┐
          │ 送达确认   │
          │ (👍 反应)  │
          └───────────┘
```

### 2.2 管理员回复流程

```
管理员在话题中回复消息
    │
    ▼
┌─────────────────┐
│  Telegram API   │
│  (Webhook 推送) │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  鉴权检查       │
│  (是否管理员)   │
└────────┬────────┘
         │ 是管理员
         ▼
┌─────────────────┐     ┌─────────────────┐
│  状态检查       │────▶│  处理备注输入   │
│  (input_note)   │     │  更新资料卡     │
└────────┬────────┘     └─────────────────┘
          │ 正常回复
          ▼
┌─────────────────┐     ┌─────────────────┐
│  命令识别       │────▶│  /trust          │
│                 │     │  加入信任列表    │
│                 │────▶│  /untrust        │
│                 │     │  移出信任列表    │
└────────┬────────┘     └─────────────────┘
          │ 普通回复
          ▼
┌─────────────────┐
│  查找对应用户   │
│  (通过 topic_id)│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  转发消息给用户 │
│  (copyMessage)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│  转发成功       │     │  转发失败       │
│                 │     │  (用户已停止Bot)│
└─────────────────┘     └─────────────────┘
```

### 2.3 验证流程

```
用户点击 /start
    │
    ▼
┌─────────────────┐
│  检查验证状态   │
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌───────┐ ┌───────────┐
│已验证  │ │ 未验证    │
│直接聊天│ │           │
└───────┘ │ 发送欢迎语│
          │           │
          │ 检查验证  │
          │ 配置      │
          └─────┬─────┘
                │
        ┌───────┼───────┐
        ▼       ▼       ▼
   ┌────────┐┌──────┐┌────────┐
   │Turnstile││reCAPTCHA││ 问答   │
   │验证    ││验证    ││验证   │
   └────┬───┘└───┬───┘└───┬────┘
        │        │        │
        └────────┴────────┘
                 │
                 ▼
          ┌───────────┐
          │ 验证通过   │
          │ 更新状态   │
          │ verified  │
          └───────────┘
```

---

## 3. 数据流图

### 3.1 数据库表关系

```
┌─────────────────────────────────────────────────────────────┐
│                      D1 数据库表结构                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐                                            │
│  │   config    │                                            │
│  │  (配置表)   │                                            │
│  │             │                                            │
│  │ key (PK)    │◄──────────────────────────────────────┐   │
│  │ value       │                                       │   │
│  └─────────────┘                                       │   │
│                                                        │   │
│  ┌─────────────┐      ┌─────────────┐                 │   │
│  │    users    │      │  messages   │                 │   │
│  │  (用户表)   │      │  (消息表)   │                 │   │
│  │             │      │             │                 │   │
│  │ user_id(PK) │◄─────│ user_id(FK) │                 │   │
│  │ user_state  │      │ message_id  │                 │   │
│  │ is_blocked  │      │ text        │                 │   │
│  │ block_count │      │ date        │                 │   │
│  │ topic_id    │─────►│             │                 │   │
│  │ user_info   │      └─────────────┘                 │   │
│  │ topic_      │                                      │   │
│  │  creating   │                                      │   │
│  └─────────────┘                                      │   │
│                                                        │   │
│  ┌─────────────────┐  ┌─────────────┐                 │   │
│  │ processed_      │  │ rateLimits  │                 │   │
│  │ updates         │  │ (限流记录)  │                 │   │
│  │ (Update去重)    │  │             │                 │   │
│  │                 │  │ key (PK)    │                 │   │
│  │ update_id(PK)   │  │ ts          │                 │   │
│  │ ts              │  │ count       │                 │   │
│  └─────────────────┘  └─────────────┘                 │   │
│                                                        │   │
│  配置项存储在 config 表中：                             │   │
│  - welcome_msg, enable_verify, captcha_mode            │   │
│  - block_keywords, keyword_responses                   │   │
│  - authorized_admins, backup_group_id                  │   │
│  - unread_topic_id, blocked_topic_id                   │   │
│  - busy_mode, busy_msg                                │
│  - enable_anti_harassment, anti_harassment_*           │   │
│  - enable_ai_anti_harassment, ai_anti_harassment_*     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────┐                                            │
│  │ user_trust  │                                            │
│  │(信任表-AI/T│                                            │
│  │  MS共享)   │                                            │
│  │             │                                            │
│  │ user_id(PK) │                                            │
│  │ username    │                                            │
│  │ trust_status│                                            │
│  │ consecutive_│                                            │
│  │  clean_count│                                            │
│  │ last_clean_ │                                            │
│  │  date       │                                            │
│  │ total_spam_ │                                            │
│  │  count      │                                            │
│  │ whitelisted_│                                            │
│  │  at/by      │                                            │
│  └─────────────┘                                            │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 缓存机制

```
┌─────────────────────────────────────────────────────────────┐
│                      内存缓存机制                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  CACHE 对象 (全局单例)                               │   │
│  │                                                     │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌────────────┐  │   │
│  │  │  data       │  │  ts         │  │  ttl       │  │   │
│  │  │  (配置缓存)  │  │  (时间戳)   │  │  (60000ms) │  │   │
│  │  └─────────────┘  └─────────────┘  └────────────┘  │   │
│  │                                                     │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌────────────┐  │   │
│  │  │  locks      │  │  admin      │  │  cleanup   │  │   │
│  │  │  (防抖锁)   │  │  (管理员缓存)│  │  (清理节流)│  │   │
│  │  │  Set类型    │  │  primarySet │  │            │  │   │
│  │  │             │  │  authSet    │  │            │  │   │
│  │  └─────────────┘  └─────────────┘  └────────────┘  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  缓存策略：                                                  │
│  1. 配置缓存：60秒 TTL，过期后重新从 D1 加载                │
│  2. 管理员缓存：60秒 TTL，减少数据库查询                     │
│  3. 内存锁：Set 类型，用于防抖（非分布式）                   │
│  4. 清理节流：避免每个请求都触发 TTL 清理                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 安全机制图

```
┌─────────────────────────────────────────────────────────────┐
│                        安全防护体系                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  第一层：Webhook 校验                                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  X-Telegram-Bot-Api-Secret-Token Header             │   │
│  │  与 env.TELEGRAM_WEBHOOK_SECRET 进行安全比较        │   │
│  │  失败返回 403 Forbidden                               │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│  第二层：幂等去重                                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  processed_updates 表记录已处理的 update_id          │   │
│  │  INSERT OR IGNORE 实现幂等                          │   │
│  │  重复请求直接返回 OK                                  │   │
│  │  自动清理 7 天前的记录                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│  第三层：限流控制                                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  用户级限流：2秒窗口，最多6条消息                     │   │
│  │  全局限流：10秒窗口，最多250条消息                    │   │
│  │  提交限流：IP 30次/分钟，UID 10次/分钟               │   │
│  │  超限返回频率限制提示                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│  第四层：ReDoS 防护                                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  正则表达式长度限制：最大 256 字符                    │   │
│  │  文本长度限制：最大 512 字符                          │   │
│  │  拒绝危险模式：(a+)+、(.*)+ 等                       │   │
│  │  失败返回 false（不匹配）                             │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│  第五层：initData 验签                                      │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  HMAC-SHA256 签名验证                                 │   │
│  │  时间戳有效期检查（默认 600 秒）                      │   │
│  │  防止伪造 Telegram WebApp 数据                        │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│  第六层：反骚扰检测                                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  用户身份检测：                                       │   │
│  │    is_bot=true → 提示，不拉黑                         │   │
│  │    username为空 → 提示，不拉黑                         │   │
│  │    is_premium=true → 放行                              │   │
│  │                                                      │   │
│  │  消息内容检测（仅已验证用户）：                         │   │
│  │    forward_from.is_bot=true → 提示+拉黑               │   │
│  │    inline_keyboard存在 → 提示+拉黑                     │   │
│  │    mention/text_mention → 提示+拉黑                    │   │
│  │                                                      │   │
│  │  配置开关：enable_anti_harassment                     │   │
│  │  └─────────────────────────────────────────────────── │   │
│  │                                                      │   │
│  │  第七层：智能反骚扰检测 (AI/Green 互斥，与黑白名单完全解耦) │   │
│  │  ┌───────────────────────────────────────────────────┐│   │
│  │  │  信任列表检查（共享同一套 user_trust，仅当日有效）  ││   │
│  │  │    trusted → 跳过检测（当日免检）                    ││   │
│  │  │    非trusted → 进入智能检测                          ││   │
│  │  │                                                     ││   │
│  │  │  ┌─── AI 反骚扰 ──────────────────────────────┐    ││   │
│  │  │  │  LLM API 检测（OpenAI Compatible）          │    ││   │
│  │  │  │  SPAM → 拉黑 + 通知 + 信任归零               │    ││   │
│  │  │  │  CLEAN → 当日信任计数+1                      │    ││   │
│  │  │  │  配置开关：enable_ai_anti_harassment        │    ││   │
│  │  │  │  环境变量：LLM_API / LLM_MODEL / LLM_KEY   │    ││   │
│  │  │  └───────────────────────────────────────────┘    ││   │
│  │  │                                                     ││   │
│  │  │  ┌─── Green 反骚扰 ───────────────────────────┐    ││   │
│  │  │  │  阿里云 Green TextModerationPlus 检测       │    ││   │
│  │  │  │  high → 拉黑 + 通知 + 信任归零              │    ││   │
│  │  │  │  medium(≥Confidence阈值) → 拉黑+通知+归零   │    ││   │
│  │  │  │  low/none → 当日信任计数+1                   │    ││   │
│  │  │  │  响应约0.2-1.5秒，支持119种语言              │    ││   │
│  │  │  │  配置开关：enable_aliyun_green              │    ││   │
│  │  │  └───────────────────────────────────────────┘    ││   │
│  │  │                                                     ││   │
│  │  │  互斥规则：开启Green自动关闭AI，反之亦然            ││   │
│  │  └───────────────────────────────────────────────────┘│   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. 部署流程图

```
┌─────────────────────────────────────────────────────────────┐
│                      部署工作流程                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  方式一：Cloudflare Dashboard 关联仓库（推荐）               │
│                                                             │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌─────────┐ │
│  │ 创建 D1  │   │ Fork 仓库 │   │修改wrangler│   │Dashboard│ │
│  │ 数据库   │──▶│         │──▶│.toml     │──▶│关联仓库 │ │
│  │记录ID   │   │         │   │填database│   │自动部署 │ │
│  └──────────┘   └──────────┘   │_id      │   └────┬────┘ │
│                                └──────────┘        │      │
│                                                     ▼      │
│                                              ┌──────────┐ │
│                                              │ 配置环境  │ │
│                                              │ 变量      │ │
│                                              └────┬─────┘ │
│                                                   ▼       │
│                                              ┌──────────┐ │
│                                              │ 设置      │ │
│                                              │ Webhook   │ │
│                                              └──────────┘ │
│                                                             │
│  方式二：Wrangler CLI                                       │
│                                                             │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌─────────┐ │
│  │ 克隆仓库 │   │wrangler  │   │wrangler  │   │wrangler │ │
│  │         │──▶│d1 create │──▶│secret put│──▶│deploy   │ │
│  │         │   │创建数据库 │   │配置变量  │   │部署     │ │
│  └──────────┘   └──────────┘   └──────────┘   └─────────┘ │
│                                                             │
│  自动初始化：                                                │
│  首次请求 ──▶ dbInit() ──▶ 自动创建所有表 ──▶ 正常运行    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. 模块依赖图

```
┌─────────────────────────────────────────────────────────────┐
│                      模块依赖关系                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  入口层                                                      │
│  ┌─────────┐                                                │
│  │ index   │                                                │
│  └────┬────┘                                                │
│       │ 依赖                                                 │
│       ▼                                                      │
│  路由层                                                      │
│  ┌─────────┐                                                │
│  │ router  │                                                │
│  └────┬────┘                                                │
│       │ 依赖                                                 │
│       ▼                                                      │
│  处理器层          安全层          API层                      │
│  ┌─────────┐      ┌─────────┐     ┌─────────┐              │
│  │handlers │◄─────│security │     │  api    │              │
│  │(update) │      │(webhook)│     │(telegram│              │
│  └────┬────┘      └─────────┘     │commands)│              │
│       │                            └────┬────┘              │
│       │ 依赖                             │                   │
│       ▼                                  │                   │
│  服务层          数据层                   │                   │
│  ┌─────────┐     ┌─────────┐            │                   │
│  │services │────▶│database │◄───────────┘                   │
│  │(relay)  │     │(users)  │                                │
│  └────┬────┘     └─────────┘                                │
│       │                                                      │
│       │ 依赖                                                 │
│       ▼                                                      │
│  工具层                                                      │
│  ┌─────────┐     ┌─────────┐                                │
│  │ utils   │     │ utils   │                                │
│  │(helpers)│     │(cache)  │                                │
│  └─────────┘     └─────────┘                                │
│                                                             │
│  依赖规则：                                                   │
│  - 上层可以依赖下层                                           │
│  - 同层之间可以互相依赖                                        │
│  - 禁止循环依赖                                               │
│  - utils 是最底层，被所有层依赖                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. 时序图

### 7.1 用户首次交互

```
用户          Telegram        Worker          D1数据库        管理员群组
 │              │               │               │               │
 │──/start────▶│               │               │               │
 │              │───Webhook────▶│               │               │
 │              │               │──dbInit()────▶│               │
 │              │               │               │──创建表──────▶│
 │              │               │               │◀──完成───────│
 │              │               │──getUser()───▶│               │
 │              │               │               │──查询用户────▶│
 │              │               │               │◀──不存在────│
 │              │               │               │──创建用户────▶│
 │              │               │               │◀──完成───────│
 │              │               │◀──返回用户────│               │
 │              │               │               │               │
 │              │               │──检查验证配置─▶│               │
 │              │               │◀──需要验证────│               │
 │              │               │               │               │
 │              │◀──发送验证按钮────────────────│               │
 │◀──显示验证页────────────────│               │               │
 │              │               │               │               │
 │──完成验证───▶│               │               │               │
 │              │───Webhook────▶│               │               │
 │              │               │──验证Token───▶│               │
 │              │               │◀──验证通过────│               │
 │              │               │──updateUser()▶│               │
 │              │               │               │──更新状态────▶│
 │              │               │               │◀──完成───────│
 │              │◀──验证通过提示───────────────│               │
 │◀──显示通过───│               │               │               │
 │              │               │               │               │
 │──发送消息───▶│               │               │               │
 │              │───Webhook────▶│               │               │
 │              │               │──getUser()───▶│               │
 │              │               │◀──已验证──────│               │
 │              │               │               │               │
 │              │               │──createTopic─▶│               │
 │              │               │               │──创建话题────▶│
 │              │               │               │◀──完成───────│
 │              │               │               │               │
 │              │               │──forwardMessage──────────────▶│
 │              │               │               │               │──显示消息
 │              │               │◀──完成───────│               │
 │              │               │──sendInfoCard───────────────▶│
 │              │               │               │               │──显示资料卡
 │              │               │◀──完成───────│               │
 │              │               │──handleInbox───────────────▶│
 │              │               │               │               │──显示未读通知
 │              │◀──送达确认(👍)────────────────│               │
 │◀──显示👍────│               │               │               │
 │              │               │               │               │
```

---

## 8. 状态机图

### 8.1 用户状态流转

```
                    ┌─────────┐
         ┌─────────│  new    │◄────────────────┐
         │         │ (新用户) │                 │
         │         └────┬────┘                 │
         │              │ /start                │
         │              ▼                       │
         │    ┌─────────────────┐               │
         │    │pending_turnstile│               │
         │    │ (等待人机验证)   │               │
         │    └────────┬────────┘               │
         │             │ 验证通过               │
         │             ▼                        │
         │    ┌─────────────────┐    ┌────────┐│
         │    │pending_verification│──▶│verified││
         │    │ (等待问答验证)    │    │(已验证)││
         │    └────────┬────────┘    └────┬───┘│
         │             │ 回答正确          │    │
         └─────────────┴──────────────────┘    │
                                               │
              ┌────────────────────────────────┘
              │ /reset (管理员命令)
              ▼
         ┌─────────┐
         │ blocked │
         │ (已屏蔽) │
         └─────────┘
```

---

## 9. 错误处理流程

```
┌─────────────────────────────────────────────────────────────┐
│                      错误处理机制                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  错误类型          处理方式          用户反馈                 │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  Webhook校验失败   返回 403          无（静默拒绝）          │
│  重复Update        返回 OK           无（幂等处理）          │
│  限流触发          返回提示          "请求过于频繁"          │
│  用户被屏蔽        返回提示          "您已被管理员屏蔽"      │
│  验证失败          返回提示          "验证失败"              │
│  话题创建失败      返回提示          "系统繁忙，请稍后重试"  │
│  消息转发失败      记录日志          "发送失败"（管理员）    │
│  数据库错误        记录日志+抛出     500 错误                │
│  未知错误          记录日志+返回     "Internal Server Error" │
│                                                             │
│  日志记录：                                                  │
│  - console.error() 输出到 Cloudflare Logs                    │
│  - 可通过 wrangler tail 查看                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 10. 监控与日志

```
┌─────────────────────────────────────────────────────────────┐
│                      监控点说明                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  关键日志输出位置：                                           │
│                                                             │
│  src/database/index.js                                       │
│  ├── SQL Fail [query]: error          # 数据库错误           │
│  └── DB Init Failed: error            # 初始化失败           │
│                                                             │
│  src/api/telegram.js                                         │
│  ├── TG API Error [method]: desc      # API 错误             │
│  └── TG API Fail [method]: error      # API 失败             │
│                                                             │
│  src/services/relay.js                                       │
│  └── Topic Create Error: error        # 话题创建失败         │
│                                                             │
│  src/router/index.js                                         │
│  └── Critical Worker Error: error     # 严重错误             │
│                                                             │
│  src/handlers/adminConfig.js                                 │
│  └── handleAdminConfig error: error   # 面板错误             │
│                                                             │
│  src/security/antiHarassment.js                               │
│  ├── [AntiHarassment] User triggered rule     # 规则触发     │
│  ├── [AntiHarassment] User intercepted        # 身份拦截     │
│  └── [AntiHarassment] Message intercepted     # 消息拦截     │
│                                                             │
│  src/security/aiAntiHarassment.js                             │
│  ├── [AiAntiHarassment] LLM API call failed  # AI API失败  │
│  └── [AiAntiHarassment] User promoted        # AI信任晋升    │
│                                                             │
│  src/security/greenAntiHarassment.js                          │
│  ├── [GreenAntiHarass] Green API call failed # Green API失败│
│  └── [GreenAntiHarass] User promoted       # Green信任晋升   │
│                                                             │
│  src/services/aiMemory.js                                    │
│  ├── [AiMemory] Failed to add correction   # 写入纠正失败   │
│  └── [AiMemory] Summarized X corrections   # 总结规则成功   │
│                                                             │
│  查看日志命令：                                               │
│  $ wrangler tail                                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 8. 长期记忆工作流 (D1 + LLM 总结)

### 8.1 记忆增强的 AI 检测流程

```
用户发送消息
    │
    ▼
┌─────────────────────────────────────────┐
│ 1. 信任度检查                             │
│    - trusted? → 跳过检测                 │
│    - 否则继续                            │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│ 2. 获取记忆上下文 (D1)                   │
│    - 查询长期规则 (ai_rules 表)          │
│    - 查询最近 3 条纠正 (ai_corrections 表) │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│ 3. 构建增强 Prompt                       │
│    [原始提示词] + [长期规则] + [热纠正]  │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│ 4. 调用 LLM API 判定                     │
│    AI 参考历史经验做出判断               │
└──────────────────┬──────────────────────┘
                   │
            ┌──────┴──────┐
            ▼             ▼
       SPAM 拦截      CLEAN 放行
            │             │
            ▼             ▼
      (无需记录到外部服务，仅执行拦截/放行)
```

### 8.2 管理员纠正流程

```
┌─────────────────────────────────────────────────────────────┐
│                    误判纠正 (SPAM → CLEAN)                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  AI 拦截消息 ──▶ 管理员收到通知                              │
│       │            │                                        │
│       │            ▼                                        │
│       │      点击 "误判，放行 (CLEAN)"                       │
│       │            │                                        │
│       │            ▼                                        │
│       │      ┌──────────────────────┐                       │
│       │      │ 1. 写入 D1 纠正记录    │                       │
│       │      │    ai_corrections 表  │                       │
│       │      └──────────┬───────────┘                       │
│       │                 │                                   │
│       │                 ▼                                   │
│       │      ┌──────────────────────┐                       │
│       │      │ 2. 解除用户黑名单     │                       │
│       │      │    is_blocked = 0    │                       │
│       │      └──────────────────────┘                       │
│       │                                                     │
│       ◀──────────── 用户恢复正常聊天                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    漏判纠正 (CLEAN → SPAM)                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  AI 放行消息 ──▶ 管理员在话题中发现垃圾                      │
│       │            │                                        │
│       │            ▼                                        │
│       │      点击 "标记为垃圾信息 (SPAM)"                    │
│       │            │                                        │
│       │            ▼                                        │
│       │      ┌──────────────────────┐                       │
│       │      │ 1. 写入 D1 纠正记录    │                       │
│       │      │    ai_corrections 表  │                       │
│       │      └──────────┬───────────┘                       │
│       │                 │                                   │
│       │                 ▼                                   │
│       │      ┌──────────────────────┐                       │
│       │      │ 2. 触发信任熔断       │                       │
│       │      │    count=0           │                       │
│       │      │    status=monitoring │                       │
│       │      └──────────┬───────────┘                       │
│       │                 │                                   │
│       │                 ▼                                   │
│       │      ┌──────────────────────┐                       │
│       │      │ 3. 执行拉黑操作       │
